# Example environment variables
PORT=3000
NODE_ENV=development
# NOTE: Replace APP_SECRET with a strong secret in production (do NOT commit real secrets)
APP_SECRET=change_me
DB_USER='react'
DB_PASSWORD='react'
#DB_SERVER='localhost'
DB_SERVER='host.docker.internal'
DB_DATABASE='react'
DB_PORT='1433'

# CORS configuration for development
# Comma-separated list of allowed origins. Include host port used by nginx (8080) and service name for container-to-container calls.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080,http://frontend:80
# Toggle to allow all origins (use only for quick local testing)
ALLOW_ALL_ORIGINS=false